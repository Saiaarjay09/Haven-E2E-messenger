# Dependencies for the hosted accounts service only (webapp/) — not
# needed to run the desktop app in haven/.
fastapi
uvicorn[standard]
bcrypt  # kept only to verify existing accounts' legacy hashes — new/upgraded hashes use argon2-cffi, see accounts_server.py
argon2-cffi
python-multipart  # needed for /api/translate's file upload (FastAPI's UploadFile/File)

# Used at runtime by /api/translate (proxies to OpenAI's Whisper API —
# see translation.js), and also by test_webapp_accounts_smoke.py's
# simulated-browser test client.
requests
